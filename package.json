{
  "name": "api-version-replay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --config site/vite.config.ts",
    "test": "cargo test && npm run test:site",
    "test:site": "vitest run --config site/vitest.config.ts",
    "build": "npm run build:site && npm run build:cli",
    "build:site": "vite build --config site/vite.config.ts && node scripts/verify-static-policy.mjs dist/site",
    "build:cli": "cargo build --release && mkdir -p dist/bin && cp target/release/vr dist/bin/vr"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.0",
    "playwright": "^1.58.2",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
